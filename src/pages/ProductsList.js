// styles
import styles from "./Container.module.css";

const ProductsList = () => {
    return (
        <main main id={styles["main"]}>
            ProductsList
        </main>
    );
};

export default ProductsList;
